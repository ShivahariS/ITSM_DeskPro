package com.itsm.notification.repository;

import com.itsm.notification.entity.ProblemReportView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProblemReportViewRepository extends JpaRepository<ProblemReportView, Long> {
}
