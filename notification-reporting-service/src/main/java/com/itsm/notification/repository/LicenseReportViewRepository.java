package com.itsm.notification.repository;

import com.itsm.notification.entity.LicenseReportView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LicenseReportViewRepository extends JpaRepository<LicenseReportView, Long> {
}
