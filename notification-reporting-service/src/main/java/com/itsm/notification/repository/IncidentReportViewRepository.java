package com.itsm.notification.repository;

import com.itsm.notification.entity.IncidentReportView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IncidentReportViewRepository extends JpaRepository<IncidentReportView, Long> {
}
